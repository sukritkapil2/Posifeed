<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CommentActivity"
    android:id="@+id/rel"
    android:padding="20dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Name"
        android:textSize="20sp"
        android:textFontWeight="700"
        android:fontFamily="@font/mclaren"
        android:id="@+id/namev"
        ></TextView>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_below="@id/namev"
        android:id="@+id/name"></EditText>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Comment"
        android:layout_below="@id/name"
        android:layout_marginTop="20dp"
        android:textSize="20sp"
        android:textFontWeight="700"
        android:fontFamily="@font/mclaren"
        android:id="@+id/commentv"></TextView>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_below="@id/commentv"
        android:id="@+id/comment"></EditText>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/note"
        android:text="NOTE: The comment should not be abusive, or insensitive towards the author. Only verified comments are shown in the feed. Verification usually takes around 2 days."
        android:fontFamily="@font/mclaren"
        android:textSize="16sp"
        android:layout_marginTop="20dp"
        android:layout_below="@id/comment"></TextView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/note"
        android:text="Post Comment"
        android:textSize="18sp"
        android:id="@+id/post"
        android:textAllCaps="false"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"></Button>

</RelativeLayout>